Icons are auto-generated on first `flutter create --platforms=web` or Netlify build.

If missing locally, run from `frontend/`:

```powershell
flutter create . --platforms=web --org com.raisadiary --project-name raisa_diary
```
