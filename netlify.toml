# Netlify — সবার জন্য web link (দ্রুত deploy)
# share-web = browser এ diary + Tahsin (localStorage, password লাগে না)

[build]
  command = "true"
  publish = "share-web"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
